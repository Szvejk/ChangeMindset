import React from 'react';
import Header from '../../Menu/Header/Header';
import Menu from '../../Menu/Nav/Menu';
const Home = () => {
	return (
		<div>
			<Menu />
			<Header />
		</div>
	);
};

export default Home;
